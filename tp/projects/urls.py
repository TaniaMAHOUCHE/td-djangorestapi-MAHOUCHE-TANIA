from django.urls import path
from . import views

urlpatterns = [
    # path('projets/', views.ProjetDeRechercheListCreate.as_view(), name='projet-liste-creation'),
    # path('chercheurs/', views.ChercheurListCreate.as_view(), name='list_chercheurs'), #name='chercheur-liste-creation'),
    # path('publications/', views.PublicationListCreate.as_view(), name='publication-liste-creation'),
    # path('list_chercheurs/', views.liste_chercheurs, name='list_chercheurs'),
]
